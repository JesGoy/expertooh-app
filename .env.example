# Copy this file to .env.local and fill in your Neon connection URL (HTTP serverless driver)
# Example format: postgres://<user>:<password>@<host>/<database>?sslmode=require
DATABASE_URL=
JWT_SECRET =
# NextAuth / Auth related (to be used later)
# NEXTAUTH_URL=
# NEXTAUTH_SECRET=
